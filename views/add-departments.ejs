<!DOCTYPE html>
<html lang="es">
<head>
    <%- include("templates/header") %>
</head>
<body>
    <div class="container">
        <div class="card mt-4">
            <div class="card-header bg-primary text-white">
                <h1>Agregar Nuevo Departamento</h1>
            </div>
            <div class="card-body">

                <% if (message) { %>
                    <div class="alert alert-info"><%= message %></div>
                <% } %>

                <form action="/test/departments/new" method="POST">
                    <div class="mb-3">
                        <label for="code" class="form-label">Código Departamento</label>
                        <input type="text" class="form-control" name="code" id="code" required>
                    </div>
                    
                    <div class="mb-3">
                        <label for="name" class="form-label">Nombre Departamento</label>
                        <input type="text" class="form-control" name="name" id="name" required>
                    </div>

                    <button type="submit" class="btn btn-success">Guardar Departamento</button>
                    <a href="/test" class="btn btn-secondary">Cancelar</a>
                    <a href="/test" class="btn btn-secondary">Volver</a>
                </form>
            </div>
        </div>
    </div>
    <footer>
        <%- include("templates/footer") %>
    </footer>
</body>
</html>
